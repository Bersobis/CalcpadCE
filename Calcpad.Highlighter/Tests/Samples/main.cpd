' Main test file with includes
#include "constants.cpd"
#include "functions.cpd"

' Use constants from included file
radius = PI * 2
area = circleArea$(radius)

' Test undefined variable
result = undefinedVar + 5

' Test balance errors
x = (1 + 2 * (3 + 4)

' Test macro usage
volume = cubeVolume$(5)
