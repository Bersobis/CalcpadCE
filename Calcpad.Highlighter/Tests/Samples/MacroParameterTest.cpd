'Test macro parameter tokenization

'Inline macro with $ params
#def double$(x$) = x$*2

'Inline macro without $ params
#def add$(a; b) = a + b

'Multiline macro with $ params
#def template$(header$; content$)
    header$ is used here
    content$ is used here too
#end def

'Multiline macro without $ params
#def calc$(x; y)
    result = x + y
    output = x * y
#end def

'Test macro parameters in comments (with $ suffix)
#def test$(stringParam$)
'stringParam$ is used in this comment
#end def

'Test macro parameters in comments (without $ suffix)
#def macro_name$(param1; param2$)
'param1 is used here and param2$ too
'content line 2 with param1 again
#end def

'Test that regular variables still work after macro definitions
z = 5
w = z + 10
